# Chart Releaser Configuration
# See: https://github.com/helm/chart-releaser/blob/main/README.md#configuration

# GitHub repository settings
owner: kenchrcum
repo: wasabi-s3-operator

# Chart directory
path: helm

# GitHub Pages settings
pages-branch: gh-pages
pages-index-path: index.yaml

# Release settings
charts-repo-url: https://github.com/kenchrcum/wasabi-s3-operator/releases/download
release-name-template: "{{ .Name }}-{{ .Version }}"

# Options
prerelease: false
debug: true
