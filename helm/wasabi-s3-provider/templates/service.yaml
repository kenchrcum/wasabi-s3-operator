apiVersion: v1
kind: Service
metadata:
  name: {{ include "wasabi-s3-provider.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "wasabi-s3-provider.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "wasabi-s3-provider.selectorLabels" . | nindent 4 }}

